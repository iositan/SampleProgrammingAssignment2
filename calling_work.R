# this is the script containing the work with the makeVector and cacheman

#  First, load the definitions -----
source("makeVector.R")
source("cacheman.R")

# Now initialize some vector ----
x <- 1:20

# Create the special vector ----
ve <- makeVector(x)
